# R pkg build artifacts
.Rproj.user/
.Rhistory
.RData
.Ruserdata
.Rcheck/
*.Rcheck/
*.Rbuildignore

# Generated by Rcpp / roxygen — NOT ignored (must be committed)
# (RcppExports.* must stay tracked; see r-package-development skill)

# Compiled artifacts
src/*.o
src/*.so
src/*.a
src/*.dylib
src/*.dll
src/*.rds
*.o
*.so

# pkgdown site built in CI
docs/

# misc
.Rproj
.Rhistory
# build tarball + RStudio project files (not source)
*.tar.gz
*.Rproj
.Rproj.user/

# knitted vignette intermediates (site built in CI from .Rmd)
vignettes/*.md
vignettes/*.html
